package it.deltax.produlytics.api.detections.business.domain.queue;

// Chiave usata per identificare una caratteristica e separare
public record CharacteristicKey(int deviceId, int characteristicId) {}
